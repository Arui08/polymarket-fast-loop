# 2026-03-07

## ResearchWang 帖子学习记录
- 来源：`https://x.com/researchwang/status/2030151219998793775?s=46`
- 可确认片段显示，这是一条关于 **OpenClaw Skills / 配置分层** 的经验帖。
- 已确认信息：
  - 作者强调：当 OpenClaw 版本高于 `3.2` 时，需要在 `openclaw.json` 中把 `tools` 权限设为 `full`，否则智能体容易退化成“只会聊天”。
  - 作者明确：`IDENTITY.md` 的职责是“对外的身份信息”。
- 形成学习：
  1. 遇到“只会聊天不会执行”的问题，先排查工具权限，而不是先怀疑模型或提示词。
  2. 文件职责继续严格分层：`IDENTITY.md` 管身份，`SOUL.md` 管行为风格，`USER.md` 管用户画像，`MEMORY.md` 管长期记忆，`memory/` 管每日上下文。
  3. 后续研究 OpenClaw 技巧时，优先关注 Skills 设计、触发描述、权限边界、分层降噪。
- 限制：本次因 X 抓取受限，未拿到完整正文，以上仅记录可稳定确认的片段，避免过度推断。

## 用户偏好补充
- 用户明确要求：后续遇到可直接安装的 skill，不需要逐个再确认；能装的直接装上。
- 执行原则：默认直接安装当前链接/仓库内可安全落地且不冲突的 skills；若涉及覆盖现有同名 skill、写入密钥、外部账户授权、付费资源或高风险配置，再单独提示。

## Binance Square 配置偏好
- 用户已提供 Binance Square OpenAPI Key，并明确要求长期配置，不要每次重复索取。
- 当前已本地保存到：`/root/.openclaw/workspace/config/binance-square.env`（仅本机使用，不入 git）。
- 以后若用户要求发 Binance Square，优先直接读取本地配置；仅在密钥失效、缺失或接口报鉴权错误时再向用户确认。
- 用户对 Square 内容要求：不要空泛，要尽量加入当日热门叙事、热门 meme、smart money / 高盈利地址等高信息密度内容。

## X/Twitter 抓取能力补充
- 用户要求安装 `https://github.com/ythx-101/x-tweet-fetcher` 作为专用 skill，用于后续直接抓取 X/Twitter 链接与 X Articles。
- 已安装到：`/root/.openclaw/workspace/skills/x-tweet-fetcher/`
- 已将 skill 描述改为更强触发：后续遇到 `x.com` / `twitter.com` 链接，或“读取/提取/总结/抓取某条推文、某篇 X 文章、某个账号时间线”时，优先使用该 skill。
- 已进一步写入长期记忆：用户发送 `x.com` / `twitter.com` 链接时，当前智能体默认优先调用 `x-tweet-fetcher`，先抓正文再做摘要/分析。
