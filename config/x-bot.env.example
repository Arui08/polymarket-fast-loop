# X auto-post config example
# Copy to: ~/.config/x-bot/.env
# Do NOT commit the real file.

# xurl app name configured locally via: xurl auth apps list
XURL_APP_NAME=default

# Optional: set a custom default identity if you use multiple apps/users in xurl
# XURL_USERNAME=your_handle
